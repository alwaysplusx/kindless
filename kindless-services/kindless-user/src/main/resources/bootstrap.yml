debug: true
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: kindless-core
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        name: kindless-core
      discovery:
        server-addr: localhost:8848
