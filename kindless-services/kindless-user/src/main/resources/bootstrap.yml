debug: true
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: kindless-user
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        name: ${spring.application.name}
      discovery:
        server-addr: localhost:8848
