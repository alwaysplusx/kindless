server:
  port: 8082

spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: kindless-provider-moment
  boot:
    admin:
      client:
        url: http://localhost:8080
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
management:
  endpoints:
    web:
      exposure:
        include: "*"
