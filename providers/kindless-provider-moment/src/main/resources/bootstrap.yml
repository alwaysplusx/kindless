spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: kindless-moment
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        name: kindless-moment
      discovery:
        server-addr: localhost:8848


