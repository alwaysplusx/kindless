schema {
    query: Query
}

type Query {
    moments: [Moment]
    moment(id: Int): Moment
}

type Moment {
    id: Int
    userId: Int
    content: String
    type: Int
    author: User
}


type User {
    id: Int
    username: String
    moments: [Moment]
}
